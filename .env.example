# Uptime Kuma 환경 변수
# 이 파일을 .env로 복사하고 값을 수정하세요

# Uptime Kuma 연결 정보
KUMA_URL=http://localhost:3001
KUMA_USERNAME=admin
KUMA_PASSWORD=your_secure_password_here

# Docker 호스트 설정 (원격 모니터링용)
# Uptime Kuma가 다른 서버에서 실행될 때, Docker 호스트의 IP를 지정
# 예: NAS에서 Uptime Kuma 실행, 개발 PC의 Docker 컨테이너 모니터링
DOCKER_HOST_IP=localhost
